name: Build and release the ESP32GreenhouseTowerDIY bin file
on:
  push:
    branches:
      - "main"
  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Cache pip
        uses: actions/cache@v3
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('**/requirements.txt') }}
          restore-keys: ${{ runner.os }}-pip-

      - name: Cache PlatformIO
        uses: actions/cache@v3
        with:
          path: ~/.platformio
          key: ${{ runner.os }}-${{ hashFiles('**/lockfiles') }}

      - name: Setup Python install
        uses: actions/setup-python@v3

      - name: Install PlatformIO with Pip
        run: |
          python -m pip install --upgrade pip
          pip install --upgrade platformio
      - name: Build ESP32GreenhouseTowerDIY
        working-directory: ./Code/Platformio_Project/GreenhouseTowerDIY/
        run: pio run --environment release

  release-esp32greenhousetowerdiy:
    runs-on: ubuntu-latest
    needs: [build]
    steps:
      - name: release
        uses: softprops/action-gh-release@v1
        if: startsWith(github.ref, 'refs/tags/')
        with:
          files: ~/.pio/build/release/firmware_greenhouse_tower0.bin